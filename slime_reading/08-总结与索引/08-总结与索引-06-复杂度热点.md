---
type: index-doc
title: "08-总结与索引-06-复杂度热点"
doc_type: concept
tags:
  - slime/index-layer
  - slime/batch/30
  - slime/doc/concept
updated: 2026-07-02
---

# 06 · 复杂度热点

> 10 个热点函数 · 对齐 `UNDERSTAND-WORKFLOW` §6 · 完整内嵌代码

读热点前先完成 [[08-总结与索引-04-导读路径]] Step 1–8。

---

## 0 · 读热点前先问三个为什么

1. **为什么必须复杂？** —— RL 闭环跨 Ray/Megatron/SGLang 三运行时；colocate/offload/PD 正交叠加。
2. **为什么停在这里？** —— `generate`、`train`、`update_weights` 是三角汇点。
3. **为什么现在读它？** —— 与当前 bug 是否同路径？rollout 慢 → sglang_rollout；loss NaN → loss.py。

---

## 1 · parse_args（arguments.py）

**为何复杂：** ~2000 行；三阶段 parse + 数十组 validate + customization path。

**Code：**

```python
# 来源：slime/utils/arguments.py L1546-L1561
def parse_args(add_custom_arguments=None):
    configure_logger()
    add_slime_arguments = get_slime_extra_args_provider(add_custom_arguments)
    pre = _pre_parse_mode()
    skip_sglang = pre.debug_train_only or pre.load_debug_rollout_data is not None
    sglang_ns = None
    if not skip_sglang:
        sglang_ns = sglang_parse_args()
    # Phase 2: Parse megatron + slime args.
```

→ [[03-Arguments-Ray-02-源码走读]]

---

## 2 · RolloutManager.generate（rollout.py）

**为何复杂：** 1486 行文件；engine 拓扑、convert、split、fault injection 同文件。

**Code：**

```python
# 来源：slime/ray/rollout.py L546-L559
    def generate(self, rollout_id):
        start_time = time.time()
        self.rollout_id = rollout_id
        self.health_monitoring_resume()
        data, metrics = self._get_rollout_data(rollout_id=rollout_id)
        if self.args.debug_rollout_only:
            return
        data = self._convert_samples_to_train_data(data)
        return self._split_train_data_by_dp(data)
```

→ [[08-RolloutManager-02-源码走读]]

---

## 3 · _convert_samples_to_train_data（rollout.py）

**为何复杂：** Sample list → 张量 dict；seqlen balance、top-p replay、DP split 交织。

**Explain：** 训练侧 `get_batch` 假设 rollout 侧已完成 padding 与 advantage 预计算（部分在 train 内完成）。

→ [[20-Train-Data-02-源码走读]]

---

## 4 · generate_rollout（sglang_rollout.py）

**为何复杂：** 641 行；async HTTP、RM 并发、partial rollout、trace span。

**Code：**

```python
# 来源：slime/rollout/sglang_rollout.py L618-L633
def generate_rollout(
    args: Namespace, rollout_id: int, data_source: Any, evaluation: bool = False
) -> RolloutFnTrainOutput | RolloutFnEvalOutput:
    assert args.rollout_global_dataset
    if evaluation:
        ...
```

→ [[12-SGLang-Rollout-02-源码走读]]

---

## 5 · MegatronTrainRayActor.train（actor.py）

**为何复杂：** wake/sleep offload、critic 协调、routing replay fill。

**Code：**

```python
# 来源：slime/backends/megatron_utils/actor.py L380-L400
    def train(self, rollout_id: int, rollout_data_ref: Box, external_data=None):
        if self.args.debug_rollout_only:
            return None
        if self.args.offload_train:
            self.wake_up()
        with timer("data_preprocess"):
            rollout_data = self._get_rollout_data(rollout_data_ref)
        if self.role == "critic":
            result = self.train_critic(rollout_id, rollout_data)
        else:
            self.train_actor(rollout_id, rollout_data, external_data=external_data)
        if self.args.offload_train:
            del rollout_data
            self.sleep()
        return result
```

→ [[19-Train-Step-02-源码走读]]

---

## 6 · compute_advantages_and_returns（loss.py）

**为何复杂：** 多 estimator 分支 + CP all-gather + custom hook。

**Code：**

```python
# 来源：slime/backends/megatron_utils/loss.py L661-L676
def compute_advantages_and_returns(args: Namespace, rollout_data: RolloutBatch) -> None:
    """Compute advantages and returns in-place based on `args.advantage_estimator`.
    Supported methods: "grpo", "gspo", "cispo", "ppo",
    "reinforce_plus_plus", and "reinforce_plus_plus_baseline".
    If ``args.custom_advantage_function_path`` is set, it is called after KL computation
    and must populate ``rollout_data["advantages"]`` and ``rollout_data["returns"]``.
    """
```

→ [[21-Loss-Advantages-02-源码走读]]

---

## 7 · policy_loss_function（loss.py）

**为何复杂：** PPO/GSPO clip + TIS/ICEPOP + KL + CP gather。

**Code：**

```python
# 来源：slime/backends/megatron_utils/loss.py L881-L893
def policy_loss_function(
    args: Namespace,
    batch: RolloutBatch,
    logits: torch.Tensor,
    sum_of_sample_mean: Callable[[torch.Tensor], torch.Tensor],
) -> tuple[torch.Tensor, dict[str, torch.Tensor]]:
    """Compute policy loss (PPO/GSPO) and metrics.
    Calculates PPO-style clipped policy gradient loss. For GSPO, gathers
    full sequences via context-parallel all-gather before computing per-sample KL.
    """
```

→ [[21-Loss-Advantages-02-源码走读]] · [[22-Loss-Policy-00-MOC]]

---

## 8 · update_weights（actor.py）

**为何复杂：** fault tolerance recover、多 engine NCCL group、colocate tensor 路径。

**Code：**

```python
# 来源：slime/backends/megatron_utils/actor.py L583-L606
    def update_weights(self) -> None:
        if self.args.debug_train_only or self.args.debug_rollout_only:
            return
        if self.args.use_fault_tolerance:
            if dist.get_rank() == 0:
                ray.get(self.rollout_manager.recover_updatable_engines.remote())
            dist.barrier(group=get_gloo_group())
        (
            rollout_engines,
            rollout_engine_lock,
            num_new_engines,
            engine_gpu_counts,
            engine_gpu_offsets,
            all_engine_actors,
        ) = ray.get(self.rollout_manager.get_updatable_engines_and_lock.remote())
        if not rollout_engines and not reconnect_rollout_engines:
            if dist.get_rank() == 0:
                logger.info("No updatable SGLang engines are running; skip weight update.")
            return
```

→ [[24-WeightSync-Dist-02-源码走读]]

---

## 9 · SGLangEngine.update_weights（sglang_engine.py）

**为何复杂：** 691 行；NCCL process group、HF iterator、server reload 协调。

→ [[15-SGLang-Engine-02-源码走读]]

---

## 10 · TrajectoryManager（trajectory.py）

**为何复杂：** 509 行；多 adapter、span 嵌套、Sample 转换。

→ [[27-Agent-Trajectory-02-源码走读]]

---

## 热点文件行数表

| 文件 | ~行数 | 建议批次 |
|------|-------|----------|
| `arguments.py` | 2000 | 03–04 |
| `rollout.py` | 1486 | 08–09 |
| `loss.py` | 1322 | 21–22 |
| `model.py` | 968 | 18–19 |
| `megatron_server.py` | 770 | 30 |
| `sglang_engine.py` | 691 | 15 |
| `actor.py` | 683 | 17–19 |
| `sglang_rollout.py` | 641 | 12 |
| `trajectory.py` | 509 | 27 |

---

## 导航

- [[08-总结与索引-07-可观测与CI]]
- [[08-总结与索引-05-checkpoint]]
